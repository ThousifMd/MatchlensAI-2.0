{
  "name": "matchlens-ai",
  "version": "1.0.0",
  "description": "Matchlens AI - Profile optimization service",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
